name: Atualizar tabelas dos jogadores

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'  # Executa todo dia às 3h da manhã UTC

jobs:
  atualizar-tabelas:
    runs-on: ubuntu-latest

    steps:
    - name: Clonar repositório
      uses: actions/checkout@v3

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Instalar dependências
      run: pip install requests beautifulsoup4

    - name: Rodar script de coleta múltipla
      run: python coletar_multiplos.py

    - name: Commit e push dos arquivos gerados
      env:
        GH_TOKEN: ${{ secrets.GH_PAT }}
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"
        git add public/*.html
        git commit -m "🔄 Atualização automática das tabelas dos jogadores" || echo "Nada para comitar"
        git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git HEAD:main
