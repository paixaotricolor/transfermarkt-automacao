name: Atualizar tabela do Rafael

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'

jobs:
  atualizar-html:
    runs-on: ubuntu-latest

    steps:
    - name: Clonar repositório
      uses: actions/checkout@v3

    - name: Instalar Python e dependências
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Instalar bibliotecas
      run: pip install requests beautifulsoup4

    - name: Rodar o script Python
      run: python coletar_dados.py

    - name: Fazer commit do HTML gerado
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add public/tabela_desempenho.html
        git commit -m "🔁 Atualização automática da tabela"
        git push
